using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IsometricRender : MonoBehaviour
{
    void Update()
    {
        //GetComponent<Renderer>().sortingOrder = Mathf.RoundToInt(transform.position.y * 1f) * -1;
    }
}
