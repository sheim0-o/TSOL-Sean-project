using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Keyhole : MonoBehaviour
{
    /*
    public Item keyItem;
    public bool isKeyForCatCage;
    public CutsceneManager cutsceneManager;

    public void Unlock(Item key)
    {
        if(key.actionType == ActionType.Open && key == keyItem)
        {
            Destroy(this.gameObject);
            InventorySystem.instance.DropItem(key);
            if (isKeyForCatCage)
            {

                cutsceneManager.LoadCutscene(1, isKeyForCatCage);
            }
        }
    }
    */
}
